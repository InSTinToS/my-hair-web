import { styled } from '@app/styles'

import { Trigger as DefaultTrigger } from '@radix-ui/react-collapsible'

export const Trigger = styled(DefaultTrigger, {
  backgroundColor: 'transparent',
  border: 'none'
})
